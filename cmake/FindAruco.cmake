IF (WIN32)
	SET(ARUCO_ROOT_DIR
		"$ENV{Aruco_ROOT_DIR}")

	SET(ARUCO_INCLUDE_DIRS ${ARUCO_ROOT_DIR}/include)

	FIND_LIBRARY(ARUCO_LIBRARIES Names aruco3011 HINTS ${ARUCO_ROOT_DIR}/lib)

	MARK_AS_ADVANCED(ARUCO_LIBRARIES)

	INCLUDE(FindPackageHandleStandardArgs)
	FIND_PACKAGE_HANDLE_STANDARD_ARGS(Aruco DEFAULT_MSG ARUCO_LIBRARIES ARUCO_INCLUDE_DIRS)

	MARK_AS_ADVANCED(ARUCO_LIBRARIES ARUCO_INCLUDE_DIRS)
ELSE()
    
ENDIF()